<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeighborJob"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PantallaLogin"/>
        <activity android:name=".Register"/>
        <activity android:name=".PrincipalLogeado"/>
        <activity android:name=".CreateJobMain"/>

        <activity android:name=".AvalaibleJobs"/>
        <activity android:name=".ActiveJobs"/>
        <activity android:name=".JobHistory"/>


        <activity android:name=".TypeJob"/>

        <activity android:name=".RegisterEmployeer"/>

    </application>

</manifest>